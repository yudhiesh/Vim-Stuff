# editor / tool state
.DS_Store

# herdr skill: vendored deps + accidental nested copy of the upstream repo
skills/herdr/vendor/
skills/herdr/herdr/

# plugin caches: metadata is tracked, fetched plugin content is machine-local
plugins/cache/
plugins/node_modules/
