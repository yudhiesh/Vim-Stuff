interface:
  display_name: "PR Ready Gate"
  short_description: "Run a strict draft-to-ready PR readiness review"
  default_prompt: "Review this pull request before moving from draft to ready and return a pass/fail readiness verdict with required fixes."
