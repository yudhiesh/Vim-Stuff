# pi

This subtree tracks only `agent/extensions/subagent/config.json`.

Everything else under the live `~/.pi/agent` tree is runtime state, secrets, caches, or generated integration output and stays untracked.
